# ── Secretos y entorno (NUNCA subir) ──
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Credenciales sueltas
*.pem
*.key
credentials.json
secrets.json

# ── Dependencias y build ──
node_modules/
.next/
out/
.vercel
dist/
build/
.turbo/
*.tsbuildinfo

# ── Fotos locales (fallback sin Cloudinary) ──
public/uploads/*
!public/uploads/.gitkeep

# ── Logs y caché ──
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
.cache/

# ── OS / editor ──
.DS_Store
Thumbs.db
Desktop.ini
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
*.swo

# ── Cursor / agentes (opcional, no necesario en repo) ──
.cursor/
